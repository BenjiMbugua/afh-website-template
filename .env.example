# Payload CMS
DATABASE_URI=postgresql://payload:password@localhost:5432/jacecare
PAYLOAD_SECRET=your-secret-here-min-32-chars

# Site URL (used by Payload for admin links and media URLs)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Contact form (optional — falls back to console logging)
CONTACT_EMAIL=your@email.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
